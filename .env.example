# Copy to .env and fill in real values. NEVER commit .env.
# Target environment (auth2 = staging)
BASE_URL=https://auth2.tlslogistics.org

# RBAC test users — full access (create/edit on all channels)
FULL_ACCESS_EMAIL=
FULL_ACCESS_PASSWORD=

# View-only access
VIEW_ONLY_EMAIL=
VIEW_ONLY_PASSWORD=

# Create access (subset of channels)
CREATE_ACCESS_EMAIL=
CREATE_ACCESS_PASSWORD=
